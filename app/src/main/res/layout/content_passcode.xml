<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.scyoung.puzzlemethis.PasscodeActivity"
    tools:showIn="@layout/activity_passcode">

    <TextView
        android:id="@+id/simulatePasscode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:text="Simulate passcode success"/>

    <LinearLayout
        android:id="@+id/buttonContainer"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_centerInParent="true"
        android:layout_below="@id/simulatePasscode"
        android:orientation="horizontal">

        <Button
            android:id="@+id/trueButton"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:text="True"
            android:onClick="setPreferenceTrue"/>

        <Button
            android:id="@+id/falseButton"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:text="False"
            android:onClick="setPreferenceFalse"/>

    </LinearLayout>

</RelativeLayout>
